<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">

   <ImageView
       android:id="@+id/song_img"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:background="@drawable/shape_for_recntlayout"
        app:civ_border_width="30dp"
       android:layout_marginLeft="10dp"
       android:layout_marginTop="10dp"
        app:civ_border_color="@color/black"
        app:civ_border_overlay="true"
        app:circularflow_radiusInDP="30dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>
    <TextView
        android:id="@+id/song_nme"
        android:layout_width="80dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:maxLines="2"
        android:textColor="@color/white"
        android:text="binding light"
        android:ellipsize="end"
        android:gravity="center"
        app:layout_constraintLeft_toLeftOf="@id/song_img"
        app:layout_constraintRight_toRightOf="@id/song_img"
        app:layout_constraintTop_toBottomOf="@id/song_img"/>

</androidx.constraintlayout.widget.ConstraintLayout>